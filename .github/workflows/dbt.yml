name: dbt

on:
  pull_request:
    paths:
      - 'dbt/**'

jobs:
  dbt-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Placeholder
        run: echo "dbt project not yet built — this will run dbt build/test once Phase 3 starts"
